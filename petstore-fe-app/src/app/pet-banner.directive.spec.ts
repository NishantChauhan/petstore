import { PetBannerDirective } from './pet-banner.directive';

describe('PetBannerDirective', () => {
  it('should create an instance', () => {
    const directive = new PetBannerDirective();
    expect(directive).toBeTruthy();
  });
});
